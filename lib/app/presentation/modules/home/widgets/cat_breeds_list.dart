
import 'package:flutter/material.dart';

class CardBreedsList extends StatelessWidget {
  const CardBreedsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}